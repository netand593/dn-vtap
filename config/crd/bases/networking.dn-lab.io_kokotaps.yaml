---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: kokotaps.networking.dn-lab.io
spec:
  group: networking.dn-lab.io
  names:
    kind: Kokotap
    listKind: KokotapList
    plural: kokotaps
    singular: kokotap
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Kokotap is the Schema for the kokotaps API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KokotapSpec defines the desired state of Kokotap
            properties:
              destIp:
                description: IP address of the host which will receive the captured
                  packets
                type: string
              mirrorType:
                description: Type of mirror traffic
                type: string
              namespace:
                description: Namespace of the pod
                type: string
              podName:
                description: Pod's name in which the packet capture should be done
                type: string
              vxlanID:
                description: VXLAN ID
                format: int32
                type: integer
            required:
            - destIp
            - mirrorType
            - namespace
            - podName
            - vxlanID
            type: object
          status:
            description: KokotapStatus defines the observed state of Kokotap
            properties:
              id:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file Conditions []metav1.Condition `json:"conditions,omitempty"
                  patchStrategy:"merge" patchMergeKey:"type" protobuf:"bytes,1,rep,name=conditions"`'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
